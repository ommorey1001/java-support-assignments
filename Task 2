for (Transaction tx : transactions) {
    if (tx.isInvalid()) {
        transactions.remove(tx);
    }
}



Minimal code change to resolve this safely

Iterator<Transaction> itr = transactions.iterator();

while (itr.hasNext()) {
    Transaction tx = itr.next();

    if (tx.isInvalid()) {
        itr.remove();
    }
}
